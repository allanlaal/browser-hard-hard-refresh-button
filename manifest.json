{
  "manifest_version": 2,
  "name": "HARD Hard Refresh",
  "version": "1.0",
  "description": "Adds a refresh button to clear cookies, cache, sessions and local storage of the current tab before reloading it. will not delete form autocomplete nor password data",
  "permissions": [
    "activeTab",
    "browsingData"
  ],
  "background": {
    "scripts": ["background.js"]
  },
  "browser_action": {
    "default_title": "HARD Hard Refresh",
    "default_icon": "icon.svg"
  }
}
